name = "msdesign-telegram-proxy"
main = "telegram-proxy.js"
compatibility_date = "2024-01-01"

[vars]
# Non-secret variables can go here

# Secrets (set via: wrangler secret put TELEGRAM_BOT_TOKEN && wrangler secret put TELEGRAM_CHAT_ID)
# TELEGRAM_BOT_TOKEN = "set via wrangler secret"
# TELEGRAM_CHAT_ID = "set via wrangler secret"
